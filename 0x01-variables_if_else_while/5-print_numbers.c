#include <stdio.h>
#include <stdlib.h>

/**
 * main - The main function is the entery poin of the program
 * Return: The program returns 0 after successfull run
 */
int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
	{
		printf("%d", number);
	}
	printf("\n");

	return (0);
}
