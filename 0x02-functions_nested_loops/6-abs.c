#include "main.h"

/**
 * _abs - Computes the absolute value
 * @a: variable for checking
 * Return: return the absolute of the integer after successful run
 */
int _abs(int a)
{
	if (t > 0)
	{
		return (a);
	}
	else
		return (-a);
}
