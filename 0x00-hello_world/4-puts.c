#include <stdio.h>

/**
 * main - The main function is the entry point of the programm
 * Return: The program returns 0 after successfull run
 */
int main(void)
{
	put("Programming is like building a multilingual puzzle, 
	followed by a new line")
	
	return (0);
}
