#include <stdio.h>
#include <stdlib.h>

/**
 * main - The main function is the entery point of the program
 * Return: Te program returns 0 after successfull run
 */
int main(viod)
{
	pintf("Size of a char: %d byte(s)\n", sizeof(char));
	pintf("Size of a int: %d byte(s)\n", sizeof(int));
	pintf("Size of a long: %d byte(s)\n", sizeof(long));
	pintf("Size of a long long: %d byte(s)\n", sizeof(long long));
	pintf("Size of a float: %d byte(s)\n", sizeof(float));

	return (0);
}
